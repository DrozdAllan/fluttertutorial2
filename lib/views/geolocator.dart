import 'package:flutter/material.dart';

class Geolocator extends StatelessWidget {
  const Geolocator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
    // TODO: geolocator package
  }
}
