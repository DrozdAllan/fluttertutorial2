import 'package:flutter/material.dart';

class PairWords extends StatelessWidget {
  const PairWords({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
